specVersion: "v1"
serviceId: "peeriq"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "peeriq-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
